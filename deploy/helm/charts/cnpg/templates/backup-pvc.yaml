{{- if .Values.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.global.appMode }}-{{ include "cnpg.fullname" . }}-backup-pvc
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cnpg.labels" . | nindent 4 }}
spec:
  storageClassName: {{ .Values.global.appMode }}-{{ include "cnpg.fullname" . }}-backup
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.backup.nfs.storage | default "200Mi" }}
{{- end }}