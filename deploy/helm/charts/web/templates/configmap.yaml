apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "web.fullname" . }}-config
  annotations:
    "helm.sh/hook": pre-upgrade,pre-install
    "helm.sh/hook-weight": "-10"
  labels:
    {{- include "web.labels" . | nindent 4 }}
data:
  DATABASE_TYPE: "{{ .Values.global.dbType }}"
  DATABASE_NAME: "{{ .Values.global.databaseName | default .Release.Name }}"
  DATABASE_USER: "{{ .Values.global.databaseUser | default .Release.Name }}"
  NAMESPACE: "{{ .Release.Namespace }}"
  DJANGO_SETTINGS_MODULE: "_config.settings"

  AWS_STORAGE_BUCKET_NAME: "{{ .Values.imageConfigMaps.awsStorageBucketName }}"
  AWS_S3_ENDPOINT_URL: "{{ .Values.imageConfigMaps.awsS3EndpointUrl }}"
  AWS_REGION: "{{ .Values.imageConfigMaps.awsRegion }}"
  AWS_QUERYSTRING_EXPIRE: "{{ .Values.imageConfigMaps.awsQuerystringExpire }}"
